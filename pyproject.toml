[project]
name = "mcp-injector"
version = "3.3.6"
description = "Surgical MCP injection for IDEs."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "psutil>=5.9.0",
]

[project.scripts]
mcp-surgeon = "mcp_injector:main"

[tool.setuptools]
py-modules = ["mcp_injector"]
